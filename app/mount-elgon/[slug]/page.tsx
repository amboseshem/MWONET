import {notFound} from "next/navigation";import PageHero from "../../components/PageHero";import {elgonTopics,images} from "../../data/site";
export function generateStaticParams(){return elgonTopics.map(t=>({slug:t.slug}))}
export default async function ElgonTopic({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const t=elgonTopics.find(x=>x.slug===slug);if(!t)return notFound();const img=slug.includes("water")?images.water:slug.includes("farming")||slug.includes("erosion")?images.farming:images.forest;return <><PageHero eyebrow="Mount Elgon" title={t.title} text={t.summary} image={img}/><section className="section"><div className="container intro-grid"><div><p className="eyebrow">LANDSCAPE STORY</p><h2>Understanding the challenge and the opportunity.</h2></div><div><p className="big-copy">This dedicated page is ready for verified environmental information, MWONET field observations, authentic photography, maps, project interventions and community voices.</p><p className="notice">Environmental facts and claims will be verified before final publication.</p></div></div></section><section className="section tint"><div className="container card-grid three"><article className="info-card"><h3>What we see</h3><p>Field conditions, environmental pressures and community realities.</p></article><article className="info-card"><h3>Why it matters</h3><p>Connections to livelihoods, water, biodiversity and resilience.</p></article><article className="info-card"><h3>What can be done</h3><p>Practical conservation, restoration and partnership actions.</p></article></div></section></>}
