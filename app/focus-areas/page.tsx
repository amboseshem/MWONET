import Link from "next/link";import PageHero from "../components/PageHero";import {images,programs} from "../data/site";
export default function FocusAreas(){return <><PageHero eyebrow="Our work" title="Focus areas for meaningful change." text="Six connected program areas provide the foundation for MWONET's environmental, youth and community work." image={images.seedlings}/><section className="section"><div className="container card-grid three">{programs.map((p,i)=><article className="program-card" key={p.slug}><span className="icon">{p.icon}</span><small>0{i+1}</small><h3>{p.title}</h3><p>{p.summary}</p><Link href={`/focus-areas/${p.slug}`}>View program →</Link></article>)}</div></section><section className="section tint"><div className="container intro-grid"><div><p className="eyebrow">PROGRAM DEVELOPMENT</p><h2>Ready for the official focus areas.</h2></div><div><p>When MWONET supplies its constitution and focus-area documents, each program page will be refined with objectives, activities, beneficiaries, targets, projects, evidence and media.</p></div></div></section></>}
