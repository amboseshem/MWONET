import Link from "next/link";
import {publicPages} from "../../_data/admin";

function editorSlug(path:string){return path==="/"?"home":path.replace(/^\//,"").replaceAll("/","--")}
export default function AdminPages(){return <main className="admin-content"><div className="admin-page-head"><div><span className="admin-breadcrumb">MWONET Admin / Website</span><h1>Pages</h1><p>WordPress-style page management foundation for the existing MWONET website. Open any page to prepare content, hero, SEO and publishing controls.</p></div><button className="admin-primary" type="button">+ New page</button></div><section className="admin-panel"><div className="admin-panel-head"><h2>Website pages</h2><span className="admin-badge">{publicPages.length} core pages</span></div><div className="admin-table-wrap"><table className="admin-table"><thead><tr><th>Page</th><th>URL</th><th>Status</th><th>Editor</th><th>Public view</th></tr></thead><tbody>{publicPages.map(([name,path])=><tr key={path}><td><strong>{name}</strong><small>MWONET public page</small></td><td><code>{path}</code></td><td><span className="admin-badge">Published</span></td><td><Link className="admin-action-link" href={`/admin/pages/${editorSlug(path)}`}>Edit page →</Link></td><td><Link className="admin-action-link" href={path} target="_blank">View ↗</Link></td></tr>)}</tbody></table></div></section><section className="admin-panel"><div className="admin-panel-head"><h2>Publishing workflow</h2></div><div className="admin-quick-grid"><span className="admin-section-note">Draft</span><span className="admin-section-note">Review</span><span className="admin-section-note">Published</span><span className="admin-section-note">Scheduled</span></div><p className="admin-section-note">Revision history and live database publishing are prepared as the next CMS phase. Until the database is connected, this screen will not pretend to persist page edits.</p></section></main>}
