import Link from "next/link";
import {requireAdmin} from "../_lib/auth";
import {adminModules} from "../_data/admin";
import {adminLogout} from "../login/actions";

const sidebarOrder=["pages","media","posts","events","programs","projects","members","users","leadership","forms","themes","plugins","navigation","seo","roles","audit-logs","settings","system"];

export default async function ProtectedAdminLayout({children}:{children:React.ReactNode}){const session=await requireAdmin();const modules=sidebarOrder.map(slug=>adminModules.find(m=>m.slug===slug)).filter(Boolean) as typeof adminModules;return <div className="admin-shell"><aside className="admin-sidebar"><Link href="/admin" className="admin-brand"><img src="/mwonet-logo.svg" alt="MWONET"/><span><strong>MWONET</strong><small>Admin control centre</small></span></Link><nav className="admin-sidebar-nav"><div className="admin-nav-section">Dashboard</div><Link href="/admin"><span>⌂</span>Overview</Link><div className="admin-nav-section">Management</div>{modules.map(m=><Link key={m.slug} href={`/admin/${m.slug}`}><span>{m.icon}</span>{m.label}</Link>)}</nav></aside><section className="admin-main"><div className="admin-mobile-nav"><Link href="/admin">Dashboard</Link><Link href="/admin/pages">Pages</Link><Link href="/admin/media">Media</Link><Link href="/admin/members">Members</Link><Link href="/admin/themes">Themes</Link><Link href="/admin/plugins">Plugins</Link></div><header className="admin-topbar"><div className="admin-search"><input aria-label="Search admin" placeholder="Search pages, members, projects..."/></div><div className="admin-topbar-actions"><Link className="admin-pill" href="/" target="_blank">View website ↗</Link><span className="admin-pill">Super Admin</span><span className="admin-avatar">{session.email.slice(0,1).toUpperCase()}</span><form action={adminLogout}><button className="admin-secondary" type="submit">Sign out</button></form></div></header>{children}</section></div>}
