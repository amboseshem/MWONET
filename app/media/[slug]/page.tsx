import {notFound} from "next/navigation";import PageHero from "../../components/PageHero";import {images} from "../../data/site";
const data:{[key:string]:[string,string,string]}={photos:["Photo Gallery","Authentic MWONET field photography will be organized here.",images.forest],videos:["Videos","Interviews, field documentaries and project clips will be embedded here.",images.community],projects:["Projects","Project galleries, milestones and results will be documented here.",images.seedlings],events:["Events Media","Photos and video from trainings, campaigns and community events.",images.farming]};
export function generateStaticParams(){return Object.keys(data).map(slug=>({slug}))}
export default async function MediaDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=data[slug];if(!d)return notFound();return <><PageHero eyebrow="Media" title={d[0]} text={d[1]} image={d[2]}/><section className="section"><div className="container gallery-placeholder"><div><span>MEDIA SLOT 01</span></div><div><span>MEDIA SLOT 02</span></div><div><span>MEDIA SLOT 03</span></div><div><span>MEDIA SLOT 04</span></div><div><span>MEDIA SLOT 05</span></div><div><span>MEDIA SLOT 06</span></div></div><p className="container notice">Official MWONET media will replace these structured placeholders as files are supplied.</p></section></>}
