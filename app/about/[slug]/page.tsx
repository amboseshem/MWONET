import {notFound} from "next/navigation";import PageHero from "../../components/PageHero";import {images} from "../../data/site";
const sections:{[k:string]:[string,string]}={history:["Our History","The official MWONET history will document the organization’s formation, community roots, milestones and growth."],vision:["Our Vision","MWONET’s approved vision statement will be published here exactly as adopted by the organization."],mission:["Our Mission","MWONET’s approved mission statement will define its purpose, people and approach."],objectives:["Our Objectives","Constitutional objectives will be presented clearly, with links to the programs that put them into practice."],"core-values":["Our Core Values","The values that guide integrity, inclusion, stewardship, partnership and accountability will be presented here."]};
export function generateStaticParams(){return Object.keys(sections).map(slug=>({slug}))}
export default async function AboutDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=sections[slug];if(!d)return notFound();return <><PageHero eyebrow="About MWONET" title={d[0]} text={d[1]} image={images.community}/><section className="section"><div className="container legal"><p className="eyebrow">OFFICIAL CONTENT</p><h2>Ready for the approved organization documents.</h2><p className="big-copy">This dedicated page ensures the final website does not compress important organizational information into a single long page. Once the constitution is supplied, the verified wording will replace this placeholder.</p></div></section></>}
