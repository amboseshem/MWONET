import {notFound} from "next/navigation";import PageHero from "../../components/PageHero";import {images} from "../../data/site";
const ways:{[k:string]:[string,string]}={volunteer:["Volunteer with MWONET","Contribute your time, skills and energy to community and environmental initiatives."],partner:["Partner with MWONET","Explore institutional, technical, funding and implementation partnerships."],support:["Support the work","Help strengthen specific programs, campaigns and community initiatives."],donate:["Support through giving","Donation channels will only be published after official organizational approval and verification."]};export function generateStaticParams(){return Object.keys(ways).map(slug=>({slug}))}
export default async function Way({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=ways[slug];if(!d)return notFound();return <><PageHero eyebrow="Get involved" title={d[0]} text={d[1]} image={images.community}/><section className="section"><div className="container form-layout"><div><p className="eyebrow">EXPRESS INTEREST</p><h2>Start the conversation.</h2><p>Use the official contact channel while structured online applications are being finalized.</p></div><form className="interest-form" action="mailto:info@mwonet.org" method="post" encType="text/plain"><label>Name<input name="name" required/></label><label>Email<input name="email" type="email" required/></label><label>Phone<input name="phone"/></label><label>Message<textarea name="message" rows={5} required/></label><button className="button primary" type="submit">Contact MWONET</button></form></div></section></>}
